server:
  port: 7082
  address: 127.0.0.1

spring:
  application:
    name: customer-service

  datasource:
    url: jdbc:mysql://localhost:3306/insurance_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Maggi@10128
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  cloud:
    openfeign:
      circuitbreaker:
        enabled: true   # ✅ REQUIRED for Feign + Resilience4j
